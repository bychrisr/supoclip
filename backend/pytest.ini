[pytest]
addopts = -q
testpaths = tests
asyncio_mode = auto
markers =
    unit: fast, isolated unit tests
    integration: integration-ish tests (still mocked; no real infra)
    slow: slow tests (should be avoided in CI)

